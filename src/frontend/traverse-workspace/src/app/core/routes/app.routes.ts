// src/app/core/routes/app.routes.ts
//
// APP_ROUTES — Angular router route definitions (lazy-loaded).
//
// PHASE 4 TEMPORARY STUB: This file contains an empty route array to satisfy
// the app.config.ts import during Phase 4. The full route table is created
// in Phase 8 once all lazy-loaded components exist.
//
// Why stub it here: app.config.ts needs APP_ROUTES to compile. The components
// that APP_ROUTES will reference (AppShellComponent, DashboardComponent,
// LoginPlaceholderComponent, UnauthorizedComponent) are created in Phases 6–8.
// An empty array satisfies the Routes type and allows the build to proceed.
//
// Replaced by: Phase 8 Step 8.5 (full route table with lazy imports)

import { Routes } from '@angular/router';

/**
 * Application route table. Phase 4 stub — empty array.
 * Full route table added in Phase 8 when all components exist.
 */
// Phase 0 stub — replaced in Phase 8 with full lazy-loaded route table
export const APP_ROUTES: Routes = [];
