// GlobalUsings centralises namespaces required by every type in this project.
// Keeping these implicit reduces visual noise in source files; the project has no
// other transitive `global using` sources because it depends on no NuGet packages
// or other projects, so the set here is the complete implicit-using surface.
global using System;
global using System.Collections.Generic;
